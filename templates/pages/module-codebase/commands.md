---
title: <module name> commands
type: module-page
summary: "<One line: what you can run.>"
audience: [P1]
updated: <YYYY-MM-DD>
sources: [<source-id>]
---

# <module name> commands

Every entry point you can run, checked against its own `--help` on the date in the
frontmatter.

| Command | What it does | Key options |
|---|---|---|
| `<command>` | <one line> | `<options>` |

## Notebook entry points

<Calls that are used interactively rather than from a shell.>
